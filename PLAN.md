# خطة العمل – نظام ERP احترافي للشركات

---

## رؤية المنتج

- **منتج يُباع للشركات:** برنامج ERP احترافي، جاهز للإنتاج، بدون ثغرات ظاهرة ولا نقص في الوظائف الأساسية.
- **التشغيل عند الزبون:** نسخة **Desktop** تُثبَّت على أجهزة العميل (Windows)، تعمل مع أو بدون إنترنت.
- **المزامنة:** بيانات محلية + مزامنة دورية مع السيرفر عندما يتوفر اتصال (لأن العملاء غالباً بدون إنترنت دائم).
- **النسخ الاحتياطي:** نسخ احتياطي تلقائي للبيانات المحلية + إمكانية استرجاع.
- **التحديثات:** تحديثات للبرنامج من السيرفر (مع الحفاظ على البيانات أو استعادتها من backup إذا لزم).

هذه الخطة مبنية على **الوضع الحالي** للمشروع وأولوياتك (المنتج المنزلي + المزامنة + البيك أب + التحديثات).

---

## الوضع الحالي (ملخص)

| المجال | الحالة | ملاحظة |
|--------|--------|--------|
| License API + Auto-suspend + Sync API | ✅ جاهز في الـ Backend | أساس البيع والاشتراكات والمزامنة |
| Hardware ID | ⚠️ جزئي | السيرفر جاهز؛ Desktop يحتاج تنفيذ في Electron |
| Electron | ⚠️ جزئي | نافذة + preload موجودان؛ IPC (ترخيص، تحديث، طباعة) غير منفذ في main |
| قاعدة محلية + مزامنة | ⚠️ جزئي | IndexedDB + منطق مزامنة جاهز؛ يمكن لاحقاً إضافة SQLite في Desktop إذا أردت |
| التحقق من الترخيص عند الدخول | ✅ يعمل | مع fallback لـ deviceId في الويب؛ في Desktop يحتاج hardware ID حقيقي |
| نسخ احتياطي تلقائي | ❌ غير منفذ | مطلوب للنسخة المنزلية |
| تحديث تلقائي | ❌ غير منفذ | الحزمة موجودة؛ يحتاج ربط في main |
| طباعة فواتير | ❌ غير منفذ | مطلوب للاحترافية |
| Installer (.exe) | ⚠️ أساسي | electron-builder موجود؛ يحتاج إعداد (شعار، حقوق نشر) |

---

## خارطة الطريق المُوحَّدة (حسب الأولوية)

الترتيب التالي يخدم: **برنامج منزل عند الزبون → مزامنة → بيك أب → تحديثات**.

```
المرحلة A (أساس البيع)     المرحلة B (احترافية Desktop)    المرحلة C (أمان وحماية)    المرحلة D (توسيع)
─────────────────────────   ─────────────────────────────   ───────────────────────   ─────────────────
• إكمال Electron Main      • نسخ احتياطي تلقائي            • RBAC متقدم               • تحسينات (RFID، تقارير)
  (Hardware ID + IPC)      • تحديث تلقائي                  • Audit Log
• Cron Auto-suspend        • طباعة فواتير                   • شاشة "حول البرنامج"
• Installer احترافي        • (اختياري) SQLite في Desktop
```

---

## المرحلة A: أساس البيع والمنتج المنزلي  
**الهدف:** نسخة Desktop قابلة للتثبيت عند الزبون، مع ترخيص مرتبط بجهاز ومزامنة تعمل.

| # | المهمة | الوصف | الأولوية | الحالة الحالية |
|---|--------|-------|----------|----------------|
| A.1 | **إكمال Electron Main (IPC)** | تنفيذ في `electron-main.js`: `license:get-hardware-id` (مثلاً machine-id)، حتى التحقق من الترخيص في Desktop يكون مرتبطاً بجهاز حقيقي. | 🔴 عالية | Preload جاهز؛ Main لا يرد |
| A.2 | **Cron Auto-suspend** | مهمة دورية (مثلاً يومية) تمر على الـ tenants وتحدّث `status` للشركات المنتهية اشتراكها، بدون انتظار طلب ترخيص. | 🔴 عالية | المنطق موجود عند activate/check فقط |
| A.3 | **Installer احترافي** | إعداد electron-builder: شعار الشركة، اسم المنتج، حقوق النشر، إصدار، وملف تثبيت .exe نظيف. | 🔴 عالية | أساسي فقط حالياً |
| A.4 | **التأكد من مسار الإنتاج** | تشغيل Desktop من `build/` (بدون localhost)، واختبار تسجيل الدخول + مزامنة + ترخيص على build حقيقي. | 🔴 عالية | يُختبر يدوياً |

**مخرجات المرحلة A:**  
برنامج Desktop يُثبَّت عند الزبون، ترخيص مربوط بجهاز، شركات منتهية تُعلَّق تلقائياً، وملف تثبيت جاهز للتوزيع.

---

## المرحلة B: احترافية النسخة المنزلية (مزامنة، بيك أب، تحديثات)

| # | المهمة | الوصف | الأولوية | الحالة الحالية |
|---|--------|-------|----------|----------------|
| B.1 | **نسخ احتياطي تلقائي (Backup)** | نسخ احتياطي دوري للبيانات المحلية (IndexedDB أو SQLite لاحقاً): مجلد محلي و/أو رفع إلى السيرفر. إمكانية استرجاع من واجهة الإعدادات أو عند إعادة التثبيت. | 🔴 عالية | غير منفذ |
| B.2 | **تحديث تلقائي (Auto-Update)** | استخدام electron-updater في main: التحقق من تحديث عند البدء أو كل فترة، تحميل وتثبيت مع الحفاظ على البيانات (أو استعادة من backup إذا لزم). | 🔴 عالية | الحزمة موجودة؛ غير مربوطة |
| B.3 | **طباعة الفواتير** | تنفيذ `printer:print-html` في main (مثلاً نافذة طباعة أو PDF ثم طباعة). طباعة فواتير وباركود بحجم وتنسيق مناسب. | 🔴 عالية | غير منفذ |
| B.4 | **(اختياري) SQLite في Desktop** | إذا أردت استبدال/تكامل IndexedDB بـ SQLite في Electron لتحسين الأداء أو التوافق، مع الإبقاء على نفس منطق المزامنة الحالي. | 🟡 متوسطة | حالياً IndexedDB فقط |

**مخرجات المرحلة B:**  
الزبون يشتغل بدون انترنت، بياناته تُنسخ تلقائياً، البرنامج يتحدّث بدون فقدان بيانات، والطباعة جاهزة.

---

## المرحلة C: الحماية والأمان (قبل أو بالتوازي مع البيع)

| # | المهمة | الوصف | الأولوية |
|---|--------|-------|----------|
| C.1 | **RBAC متقدم** | صلاحيات مفصلة: كاشير، مدير مخزون، محاسب، مدير نظام، إلخ. | 🔴 عالية |
| C.2 | **Audit Log** | سجل عمليات: من فعل ماذا ومتى (تعديل، حذف، طباعة، تصدير). | 🟡 متوسطة |
| C.3 | **شاشة "حول البرنامج" + حقوق النشر** | إصدار، شعار، حقوق النشر، رخصة الاستخدام. | 🟡 متوسطة |
| C.4 | **Code Obfuscation (اختياري)** | تشفير/تعمية كود JavaScript في build الإنتاج لتقليل النسخ السهل. | 🟡 متوسطة |

---

## المرحلة D: توسيع وتجربة مستخدم (لاحقاً)

| # | المهمة | الوصف | الأولوية |
|---|--------|-------|----------|
| D.1 | تحسين صفحة جرد RFID | تحسينات واجهة وسرعة. | 🟡 متوسطة |
| D.2 | RFID عبر COM في Desktop | ربط حقيقي بأجهزة RFID على منفذ تسلسلي (إذا مطلوب). | 🟡 متوسطة |
| D.3 | تكامل ESL (شاشات أسعار) | إن وُجدت أجهزة في الخطة. | 🔵 منخفضة |
| D.4 | تقارير متقدمة ورسوم بيانية | تحليلات مبيعات ومخزون. | 🔵 منخفضة |
| D.5 | إصلاحات تقنية | babel-metadata، تبعيات دائرية، إلخ. | 🔵 منخفضة |

---

## المرحلة 5 (التسعير والأعمال) – لاحقاً

صفحة تسعير، بوابة دفع، لوحة إدارة عملاء، فترة تجريبية: تُنفَّذ حسب قرارك بعد استقرار المنتج الأساسي.

---

## المرحلة 3 (تطبيق موبايل) – لاحقاً

React Native، لوحة صاحب المحل، جرد بكاميرا الموبايل، POS مبسط، تنبيهات: بعد اكتمال Desktop وانتشاره.

---

## ترتيب التنفيذ المقترح (خطوات عملية)

1. **أولاً (أساس البيع):**  
   A.1 إكمال Electron Main (Hardware ID) → A.2 Cron Auto-suspend → A.3 Installer → A.4 اختبار Build.
2. **ثانياً (ما طلبته: بيك أب + تحديثات):**  
   B.1 Backup تلقائي → B.2 Auto-Update (مع الحفاظ على البيانات أو استعادتها من backup).
3. **ثالثاً (احترافية يومية):**  
   B.3 طباعة الفواتير → C.1 RBAC → C.2 Audit Log.
4. **رابعاً:**  
   C.3 حول البرنامج + C.4 Obfuscation إن رغبت، ثم D حسب الأولوية.

---

## ملخص سريع

| المحور | الخطة |
|--------|--------|
| **المنتج** | ERP احترافي يُباع للشركات، نسخة Desktop عند الزبون. |
| **المزامنة** | موجودة (Sync API + IndexedDB/offline)؛ التأكد من عملها على build الإنتاج. |
| **النسخ الاحتياطي** | مرحلة B.1: backup تلقائي للبيانات المحلية + إمكانية استرجاع. |
| **التحديثات** | مرحلة B.2: تحديث تلقائي مع الحفاظ على البيانات (أو استعادة من backup). |
| **أول خطوة تنفيذ** | إكمال Electron Main (Hardware ID + IPC للترخيص)، ثم Cron Auto-suspend، ثم Installer. |

إذا حددت من أين تريد البدء (مثلاً A.1 أو B.1)، يمكن تفصيل خطوات تنفيذها في الملفات نفسها (electron-main.js، backend، إلخ).
