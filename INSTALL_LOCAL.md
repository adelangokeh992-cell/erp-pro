# 📦 دليل التنصيب المحلي - نظام ERP
# Local Installation Guide - ERP System

هذا الدليل يشرح كيفية تشغيل النظام على كمبيوتر Windows بدون إنترنت.

---

## 📋 المتطلبات

### البرامج المطلوبة:
1. **Node.js** (v18 أو أحدث) - https://nodejs.org/
2. **Python** (v3.10 أو أحدث) - https://www.python.org/
3. **MongoDB Community Server** - https://www.mongodb.com/try/download/community
4. **Git** (اختياري) - https://git-scm.com/

---

## 🚀 خطوات التنصيب

### الخطوة 1: تحميل الكود
```bash
# إذا لديك Git:
git clone [YOUR_REPO_URL] erp-system
cd erp-system

# أو: حمّل الكود كـ ZIP من GitHub وفك الضغط
```

### الخطوة 2: تنصيب MongoDB
1. حمّل MongoDB Community Server من الرابط أعلاه
2. ثبته مع الخيارات الافتراضية
3. تأكد أن الخدمة تعمل (تعمل تلقائياً عادة)

### الخطوة 3: إعداد Backend
```bash
cd backend

# إنشاء بيئة افتراضية
python -m venv venv

# تفعيل البيئة (Windows)
venv\Scripts\activate

# تنصيب المكتبات
pip install -r requirements.txt

# إنشاء ملف .env
echo MONGO_URL=mongodb://localhost:27017 > .env
echo DB_NAME=erp_local >> .env
echo CORS_ORIGINS=* >> .env
```

### الخطوة 4: إعداد Frontend
```bash
cd frontend

# تنصيب المكتبات
npm install
# أو
yarn install

# إنشاء ملف .env
echo REACT_APP_BACKEND_URL=http://127.0.0.1:8002 > .env
```

### الخطوة 5: تشغيل النظام

**تشغيل سريع (مُوصى به)**

1. **تشغيل الـ Backend:** من مجلد المشروع، افتح مجلد `backend` وشغّل الملف **start_backend.bat** (نقرة مزدوجة)، أو من الطرفية:
   ```bash
   cd backend
   venv\Scripts\activate
   python -m uvicorn server:app --host 0.0.0.0 --port 8002
   ```
2. **تشغيل الـ Frontend:** في نافذة طرفية أخرى:
   ```bash
   cd frontend
   npm start
   ```
3. افتح المتصفح على: **http://localhost:3000**
4. **بيانات الدخول للتجربة:**
   - مستخدم شركة: **DEMO** / **demo** / **Demo@123**
   - مدير النظام: **superadmin** / **Admin@123**

**طريقة يدوية (نافذتين)**

نافذة 1 - Backend:
```bash
cd backend
venv\Scripts\activate
uvicorn server:app --host 0.0.0.0 --port 8002 --reload
```

نافذة 2 - Frontend:
```bash
cd frontend
npm start
# أو
yarn start
```

**ملف batch للتشغيل السريع (اختياري)**

في المجلد الرئيسي يمكن استخدام `start.bat` إن وُجد، أو إنشاء واحد يشغّل Backend على المنفذ **8002** ثم Frontend:
```batch
@echo off
echo Starting ERP System...
start cmd /k "cd backend && venv\Scripts\activate && python -m uvicorn server:app --host 0.0.0.0 --port 8002"
timeout /t 3
start cmd /k "cd frontend && npm start"
echo.
echo Backend: http://127.0.0.1:8002
echo Frontend: http://localhost:3000
echo.
pause
```

---

## 📱 توصيل جهاز C6100

### الخطوة 1: معرفة عنوان IP الكمبيوتر
```cmd
ipconfig
```
ابحث عن `IPv4 Address` (مثلاً: `192.168.1.100`)

### الخطوة 2: ضبط Firewall
افتح المنافذ 3000 و 8002 في Windows Firewall:
1. افتح "Windows Defender Firewall"
2. اختر "Advanced settings"
3. أضف Inbound Rule للمنفذ 3000 (Frontend)
4. أضف Inbound Rule للمنفذ 8002 (Backend)

### الخطوة 3: تعديل Frontend .env
```env
REACT_APP_BACKEND_URL=http://192.168.1.100:8002
```
(استبدل 192.168.1.100 بعنوان IP جهازك)

### الخطوة 4: فتح التطبيق على C6100
1. تأكد أن C6100 متصل بنفس شبكة WiFi
2. افتح المتصفح على C6100
3. اذهب إلى: `http://192.168.1.100:3000`
4. سجل الدخول: DEMO / demo / Demo@123 (مستخدم شركة) أو superadmin / Admin@123 (مدير نظام)

---

## 🔧 استخدام RFID Scanner (C6100 / BX6100)

### طريقة العمل:
1. افتح صفحة "جرد المخزون" أو "الوحدات (Tags)" أو "نقطة البيع"
2. اضغط "بدء جرد جديد" (أو ما يكافئ في الصفحة)
3. وجّه القارئ نحو المنتج أو المسّ العلامة
4. اضغط زر المسح على الجهاز
5. النظام يقرأ الـ RFID Tag ويعرض المنتج

### إعداد C6100 (وضع لوحة المفاتيح):
- تأكد أن السكانر في وضع "Keyboard Wedge"
- هذا يعني أن القراءة تُرسل كضغطات لوحة مفاتيح
- معظم الأجهزة تدعم هذا الوضع تلقائياً

### إعداد BX6100 (قارئ UHF RFID):
- **الاتصال:** وصّل الجهاز بالكمبيوتر عبر كابل USB (Type-C)، أو زوّج الجهاز عبر البلوتوث من إعدادات Windows.
- **تعريف COM:** بعد الاتصال، تحقق من "إدارة الأجهزة" (Device Manager) أن منفذ COM (أو Bluetooth COM) ظاهر للجهاز. إن لم يظهر، ثبّت التعريف من الشركة المصنعة إن وُجد.
- **من داخل تطبيق Desktop (Electron) فقط:** اذهب إلى الإعدادات ← تبويب **RFID** ← قسم "قارئ عبر منفذ تسلسلي (COM)". اضغط "تحديث المنافذ"، اختر المنفذ الخاص بـ BX6100، ضبط السرعة (Baud) على **115200** أو **9600** (حسب دليل الجهاز)، ثم "بدء القراءة". بعد ذلك تظهر القراءات في الجرد والوحدات.
- **السرعة (Baud) المدعومة حسب مواصفات BX6100:** 9600، 19200، 38400، 43000، 56000، 57600، 115200.

### إضافة RFID Tags للمنتجات:
1. اذهب لصفحة "المخزون"
2. عدّل أي منتج
3. أضف الـ RFID Tag في حقل "رقم RFID"
4. احفظ

---

## 🔄 العمل بدون إنترنت (Offline Mode)

### تفعيل الوضع:
1. افتح "الإعدادات"
2. اختر تبويب "الاتصال"
3. اضغط على "أوفلاين"

### تحميل البيانات للعمل بدون إنترنت:
1. وأنت متصل بالإنترنت، اذهب للإعدادات
2. اختر تبويب "المزامنة"
3. اضغط "تحميل للعمل أوفلاين"
4. انتظر حتى يكتمل التحميل

### مزامنة البيانات:
- عند الاتصال بالإنترنت مرة أخرى
- اضغط "مزامنة الآن" في الإعدادات
- أو ستتم المزامنة تلقائياً

---

## ❓ حل المشاكل الشائعة

### المشكلة: لا يمكن الاتصال من C6100
**الحل:**
- تأكد أن الجهازين على نفس الشبكة
- تأكد من فتح المنافذ في Firewall
- جرب إيقاف Firewall مؤقتاً للاختبار

### المشكلة: RFID لا يقرأ
**الحل:**
- تأكد أن السكانر في وضع Keyboard Wedge
- تأكد أن الـ Tag مسجل في النظام
- جرب زر "محاكاة مسح" للاختبار

### المشكلة: MongoDB لا يعمل
**الحل:**
```cmd
# افتح Services
services.msc
# ابحث عن MongoDB وشغله
```

### المشكلة: خطأ CORS
**الحل:**
تأكد من إعداد CORS في backend/.env:
```env
CORS_ORIGINS=*
```

---

## 📞 الدعم

للمساعدة، تواصل معنا أو افتح issue على GitHub.

---

**آخر تحديث: فبراير 2026**
